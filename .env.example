# Paste your OpenAI API key in the file named .env (not this file).
# In .env put exactly this line (replace with your key):
# VITE_OPENAI_API_KEY=sk-your_key_here
#
# To open .env in Cursor: Cmd+P (Mac) or Ctrl+P (Windows), type:  .env
# Then choose the one in unice-portfolio (project root).
