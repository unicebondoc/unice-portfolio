# Server-side only (Vercel): set in Project Settings → Environment Variables.
# OPENAI_API_KEY=sk-your_key_here
#
# Do NOT use VITE_OPENAI_API_KEY in production — the key would be exposed in the client bundle.
# Chat calls the server at /api/chat; the server reads OPENAI_API_KEY and calls OpenAI.
